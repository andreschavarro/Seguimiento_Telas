App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Set(FiltroCritica,ComboBoxCritica.Selected.Value);
        Set(FiltroCentro,ComboBoxCentro.Selected.Value);
        Set(FiltroUbicacion,ComboBoxUbicacion.Selected.Value);
        Set(FiltroTanda,ComboBoxTanda.Selected.Value);
        Set(Filtroreferencia,ComboBoxReferencia.Selected.Value);
        Set(FiltroPrio_Maq,ComboBoxPrio_Maq.Selected.Value);
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

